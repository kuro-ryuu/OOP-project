### 1.

A) An arrival event needs to generate a new rival event to ensure a precise simulation of the randomness of customers behavior and arrival time. If entered manually without an auto-generating event function, it wouldn't be as accurate compare to the former solution.

B) By the start of a single service point, a customer should be dequeued from their queuing position.

